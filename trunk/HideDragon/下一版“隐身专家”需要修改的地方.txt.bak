
考虑OEM版的目录设置，和相关的资源文件和语言文件的设置
所有的OEM版都可以直接修改语言文件，包括版本信息，或者版本信息的RC文件使用不同的，路径在语言文件里配置
再测试隐藏窗口功能这块，尽量稳定，论坛里的BUG都测试一遍，尽量稳定。
如有可能，加入迅雷那种的弹出广告，播放FLASH广告
自动隐藏这块内存泄露的BUG再查一遍
帮助菜单要修改，快速开始减少为一个或两个，其余的都放在网页里，快速开始不再分子菜单
//加入结束被隐藏窗口的进程功能。
//在窗口比如LIST下边，加入广告条
统一中英文版







//////////////////////////////////////////////////////////////3.5版要改的地方//////////////////////////////////////////////////



//托盘图标有的没有DESTROY
//关于对话框显示注册用户名和已注册信息。 
托盘图标和窗口的分类，在自动隐藏里，需要处理。
//以管理员身份运行，托盘图标显示不正确，在有MSN的情况，或CUTEFTP的情况
英文版不注册退出
//进程路径，和填加按钮的实现，相关的数据结构的更改。
//中文的广告数据库从CN目录取
//所有的定制文本都放在INI可以编辑，但在程序里面加上对域名的判断
//BK页面，双击显示
//提示区文字前面加上　小提示：
离线状态下，网络取文件线程需要检查
//右侧边框，需要判断在XP和VISTA下，不同的边距
//统一注册码和序列号的文本，在界面
//英文版里状态栏大小的调整的代码拷到中文版里
//中文版里，所有的语言都使用同一个快速启动图标和文字
//界面上的广告链接位置需要重排，现在的文字稍微长就显示不全
论坛上有人提的BUG
全面的测试
有用户反映更改成繁体中文时有BUG，没有更改成功
可以定时清理使用痕迹
//窗口列表页需要改，增加隐藏按钮
//可以设置双击某窗口将其置于前端
//右键菜单加入将窗口置于前端
//看看新窗口的生成和显示能不能改用HOOK监控
设置界面广告文本放在单独的线程
//更改界面语言放在单独的线程
//清理痕迹需要改BUG，在VISTA下
隐藏IP需要更新新的代码
界面上加入提升权限的按钮，
//保存选项文件的位置参考这个位置，因为在VISTA下，两个权限的配置文件不配匹，检查如果是VISTA，则存在CHROME那个类型的目录。
安装程序的位置参考GOOGLE CHROME的位置
//保存次数的位置要改，直接保存在当前目录就可以
有的游戏隐藏不了，
有两个QQ有时会崩
广告超链接里面加入帮助文件或FAQ文件或使用技巧链接。
//界面文本要调整，立即隐藏窗口，将转到后台都改成立即隐藏，
//3.3//////////////////////////////////////////////////////////////////////////////////////


反复隐藏QQ时会CRASH。
//自动勾选有时不起作用。
//MSN的托盘图标的支持。
//新的托盘图标处理方法的修改。
//2000下托盘图标处理的支持。
//记忆上次自定义的针对每个窗口的快捷键。
//新的存储应该使用SQLITE数据库
SOFTREG版本解决了几个小问题，密码保护输入框的，默认的快捷键的，把相关的代码并过来。
//网络部分有内存泄露，应该解决，要么关掉，要么检查解决。
呼出主窗口时增加密码保护。
随系统启动时，有要自动后台的程序自动转到后台。
//对网游，QQ幻想，刀剑ONLINE,劲WU团，QQ三国，还有其它的网游也要测一下，不能//隐藏的要找到原因。
隐藏窗口后可以对进程进行隐藏。
//打开主页和建议和博客页都要新开IE窗口。
自动隐藏后弹出的窗口，现在会闪，要解决。
//被更改了的标题和图标有时会改回原来的。在改回原来的后，资源要释放。
窗口伪装状态保存下来。
重新显示后，仍保持原来的顺序。
开机自动隐藏托盘图标。
隐藏窗口时，有时会卡，有阻塞的窗口，会卡住。
屏保和自动关闭显示器后，会重新显示被隐藏的窗口。
//Vagga的托盘图标
//隐藏所有窗口后，将所有的托盘图标一块隐藏。
多找网游测试。
//新的图标。
//新的网站模板。
设置窗口透明度允许用户自己精确设置。
启动结束时都要有注册提示。
//自动清除无效的图标。
//禁用鼠标左右键后，连鼠标中键也给禁用了，应区分。
增加查看所有窗口的工具，可以隐藏和显示。
//后台向导的窗口列表里应显示窗口标题，多窗口可以选最长的标题或主窗口的标题。

//修复了在使用快捷键指定程序到后台运行时，有时会隐藏其它程序的托盘图标。
//界面提示文字有时会乱码。隐藏一个被勾选的，再重新显示时，提示是乱码。
//界面上鼠标的启用和禁用应该判断鼠标中键和鼠标左右键



///////////////////3.0需要修改的地方//////////////////////////////////

//将更新程序所弹出的提示对话框的父窗口改为更新程序的主窗口。

//考虑增加隐藏窗口后显示我的电脑或其它的应用程序，可自定义。

//用鼠标左键单击托盘图标有时不能将窗口后最小化。

//增加重新的显示窗口的快捷键，以防止有时其它人按下鼠标左右键后将窗口重新显示。


//解决有时在指定后台进程时所显示的窗口条目不对的问题。可能有内存泄露。

增加随系统启动自运行时可以自动隐藏上次设定的应自动隐藏（或转入到后台运行）的程序。


/*标题栏停留鼠标的问题一定要解决，考虑使用自画形式的。最好能将程序里标题栏都替换掉。或将提示对话框换成自定义的，将关闭图标去掉。*/

/*界面应该有丰富的工具提示，包括第一次启动时，最小化时，关闭时，关闭最小化，鼠标在相关控件上时。
增加了工具提示，在除点击托盘图标而最小化窗口外，其它的所有最小化窗口操作（最小化按钮和快捷键），都会在托盘图标处弹出提示。在主窗口不可见时，隐藏托图标、在托盘图标不可见时，隐藏主窗口会弹出提示对话框，显示重新显示隐身专家主窗口的操作。*/

在以后宣传和推广上(包括主页的内容和发的贴子宣传上等等)，应该着重宣传包含老板键功能。


考虑加入鼠标手势。


//可以让用户自定义重新显示窗口的快捷键。

//禁用声音时应将MIDI和WAV声音都禁用掉，现在只禁用了WAV。

更改

//可以考虑增加选项，以保留想隐藏的窗口。


//增加选项，被快速隐藏的窗口后弹出的窗口自动隐藏，应使用钩子，防止闪烁，和后台运行的后弹出的窗口同样应使用钩子捕捉。


//右键菜单项目前应该有“将此程序转到后台运行”，“记忆此窗口下次默认选中”，

//在版本历史里加入改正了自动运行不隐藏主窗口选项选中，有时却依然显示主窗口的BUG。

//皮肤一定要考虑增加。


需要支持IE7和FIREFOX等一些支持选项页的浏览器。


//性能问题一定要改进，考虑使用钩子和窗口列表的刷新形式，优化里面的一些算法，还有程序启动时读文件优化。

//将指定程序到后台运行这个功能加入到右键托盘图标和主窗口菜单中。

//内存的使用，仔细的检查一下。


//修改如果将音量图标转到后台运行时，如选中隐藏后弹出窗口，那么会隐藏任务栏的BUG。

//修改在中文版中自动运行不显示主窗口的一处BUG，和将启动时不显示托盘图标功能在正常运行也可以使用。

增加一个向导可以隐藏系统中指定的进程。



增加在隐藏窗口将进程在系统中隐藏的功能，以使用正常的方法无法查看（比如任务管理器），此功能可以做一个选项放在选项对话框里。



在窗口列表里增加右键菜单，可以指定相关的窗口到后台运行，如果含有托盘图标或其它窗口都应转到后台。和提供一个条目以选择下次不需要保留（应是一个复选形式的菜单）。

//增加自动保留功能，根据用户连续几次所选取的保留的窗口（或进程名）来确定，或可以提供一个选择文件的对话让用户指定可执行文件或直接输入进程名的对话框，或提供几个预置的进程（但默认应该不使用），这个功能需要配置的部分在选项对话框里扩展。

/////////////////////////////////////2.6版需要修改的地方////////////////////////////////////////////

//英文版和中文版后台访问不同的页面。

//解决了最小化主窗口后，跟着点右键就触发鼠标钩子，隐藏所有的窗口的问题。


//改正了单点托盘图标后的行为，如果主窗口没有在最前则将其置前，如果在最前则将其隐藏。


//改正了MISO.HDO文件的路径，使用了绝对路径，不会再出现自动运行时在别的路径建文件的问题。


//如果系统中有阻塞的程序，那么程序无法正常运行，窗口列表不能刷新，检查窗口刷新的部分。


程序结束时有时会在 closehandle(hoptionfile)处出错，检查程序里是否有提前关闭文件句柄的语句。

解决关闭窗口（主窗口，选项窗口和关于对话框，或一些提示框）出现的缓慢的问题，检查一下是不是消息循环的问题。


在主页里或自述文件里加提示“在您使用隐身专家前，花一点点时间来阅读使用手册，将节约您更多的时间，在使用过程遇到问题，您可以查阅使用手册”。

最小化主窗口有时极慢，尤其是在DELL的台式机上，看是否可以更换当前使用的最小化
隐藏方式。

退出时有错误，卡住了，检查退出时的操作，当时正在用VC调试程序。

SETUP程序中的版本信息加上，而不只是在软件名称后添加。

//改变左键单击托盘按钮的行为，如果此时主窗口没有在最前端，虽然主窗口可见，只是将主窗口置于最前端，如//果主窗口已经在最前端，单击后将主窗口隐藏。

改正在窗口列表里有显示两条相同条目的问题。

在注册说明里面加上只要在各个论坛里发布宣传隐身专家和介绍使用方法的贴子都可以获得注册码。

//检查在线更新程序只运行一个实例。

主窗口应该可调整大小。

为适应大众用户，主界面的提示多一些，比如“按下鼠标左右键隐藏窗口”，各相关控件上的工具提示应该有。

//关于对话框里“版权”页面里“技术支持”多了一个“去”。

//窗口列表里在窗口标题前添加程序的图标。


遇到没有响应的窗口，比如正在调试的窗口或没有响应的窗口，函数应该返回，接着隐藏下一个窗口，可用PostMessage 代替ShowWindow。

//选项对话框里的敏感键冲突。

启动时窗口列表禁用，防止点击后获得焦点而无法刷新。待得到完整的窗口后再启用。

加入工具提示，最小化窗口时，提示可以使用右键菜单。隐藏图标时提示显示的快捷键和显示窗口的方法，再运行一次隐身专家。

//将“在下边选取不想隐藏的窗口”，放进分组框里。


可以考虑使用过期的用户限制其升级，卸载后重新安装仍不可以升级，除非注册。

可能的话改一下验证注册的方式。

加密misc.hdo文件。

//加入显示被隐藏窗的口要输入密码的选项。

//改一下使用托盘图标显示“关于”和“选项”对话框出的位置的问题。在主窗口没显示的情况下，这两个对话框//的位置要定位在屏幕中央或屏幕下3/4处，可以考虑更改对话框窗口的属性或显示时更改位置。

考虑统计在线人数的问题，最纸应该访问一个页面，在主页里，使用一次隐身专家就访问一次，看一天下来能有多少次，和参考一下论坛里统计在线人数的例子，看怎样集成到程序里。

//原来的图片控件的问题，隐藏窗口后，鼠标变成手指状的了。原来相关的代码得改一下。


想办法改一下鼠标钩子的问题，检测同时按下要精确一些，现在的问题是，有时单键按下会
误判为同时按下。

//版权信息的时间部分也要改一下。

主页上加一个计数器，和软件下载计数器。

//在VerHistory.rtf的2.2部分里加入“制作了安装和卸载程序”。

//更新VerHistory.rtf文件。

//在快捷菜单里增加使用手册条目，访问主页上的使用手册。

//readme.rtf要更新，里面有一个字是错的。 10.lisence.txe               版权声明文件

//主页的内容全面的改一下，错别字，ALL RIGHTS RESERVED，和一些缺字的情况，在如何注册栏里
//应该增加在软件的使用上注册的步骤。

//改正访问网站如果找不到相应的文件，或服务器端关闭连接出错的问题，访问网站的线程的容
//错性应该全面的改正。
////////////////////////////////2.21需要修改的地方/////////////////////////////////////////////
//制作安装程中将所有“隐身专家”的后面加上版本号
隐藏窗口后“在线更新”菜单条改为禁用。

//内存的释放，不使用变量的删除，和一些内存的清空操作。

//将界面改成宽样式的。

//将程序现在的以后的所有提示的对话框的标题改为“隐身专家”。

//在“选项”对话框里加入“将快捷方式加入快速启动区”选项。

考虑加上公司的名字。

图标应换成Windows XP风格的，使用32位色。

可以考虑使用精致一些的皮肤。

主窗口里“在下面选择不想隐藏的窗口”应改别的颜色，方便提示。

改正在Windows XP以下的操作系统，工具提示(关于对话框里的邮箱和网址，和主窗口里的图片控件)不
能显示的问题。

//在“关于”对话框里将“Credits”选项卡改为“版权”。

//仔细修改自述文件和版本历史文件。

//改正在VC7下编译，显示“选项”对话框出错的问题。

//程序的安装和卸载应有相应的安装和卸载程序，主要是因为“操作系统启动时自动运行”//修改了注册表和便于一些用户的安装。

//内存分配应改为使用虚拟内存，稍大一些的内存都不再使用数组或堆内存。

//在“选项”对话框里提供“操作系统启动时自动运行”选项。

//在“选项”对话框里提供“隐藏窗口后禁用声音或最小化声音”的选项。

增加可以在线统计在线同时使用人数所功能。在主页上应可以统计当前同时使用的人数，一天或一月（给
定时间段里）最多使用的人数。

将主窗口下边的图片控件改为Flash控件，以加载指定位置的Flash广告条，为防止用户不在线或将缓存在
本地的Flash广告删掉，应可以进行检测，如本地广告条被删或用户没有在线而本地又没有广告条，此位
置应替换为一个图片控件，显示程序资源里自带的位图。

//修改保留窗口算法的错误，应改成进程－窗口相关的算法，而不是只依靠窗口文本
//来区分，还有窗口文本的字数的限制。

//增加自动在线检测新版本功能和在线升级功能，需要在界面上做相应的改变,和增加一个独立的更新的进程。

//修改界面上相应的版本信息，主界面上的和关于对话框里的，和程序资源的里的Version resource信息。
//////////////////////////////////////2.2版需要修改的地方////////////////////////
//“选项”对话框里的“使用鼠标左右键”复选框被点击后提示对话框标题错误。
//“关于”对话框里的“自述文件”选项卡里的虎翼网电子邮件地址错误。
//快捷键注册完后应有容错处理。
选项卡的初始显示顺序应该进行设置。
//使用ListView控件代替列表框控件。
//“关于”对话框里的超链接应加下划线和改变颜色。
//主窗口的标题栏应加入版本号。
//“关于”对话框里的编辑框的背景色应改为白色。
//“选项”对话框里修改了一些设置点“应用”后，再修改一些设置后，“取消”按钮无法恢复可用状态。
//在“关于”对话框里加入“版本历史”选项卡。
//修改一下在Windows 98下系统菜单中的最大化按钮可用的问题。
如果“保留”按钮还存在的话，那么只有“窗口列表”框内有焦点的时候它才可用。
“恢复所有”按钮在做了一次隐藏窗口后，就自动变得可用，尽管“窗口列表”里并没有保留窗口。
//“刷新间隔”编辑框应该限制宽度为1。 
//加入提示信息：选“结束所有”后，数据可能丢失。
//加入提示信息，《隐身专家》在只读介质上无法运行。
//加提示信息，在窗口的标题栏按鼠标左右键不好使。
//在不同的操作系统下安装不同的鼠标钩子。
//去掉“关于”对话框的敏感帮助按钮。
//修改版本信息。将公司改成主页地址。
添加可以将快捷方式放到桌面、快速启动区，和添加程序组的功能，在“选项”对话框里。
//在超链接处和广告条处加工具提示。
//把程序改成Release版。
最好做一个安装程序。
如果制作了安装程序后，要修改在“自述”选项卡里“包含文件”的内容。
//如果可能的话，在“关于”对话框里使用rich edit控件。
//修改DLL的版本信息。
///////////////////////////////////2.1版需要修改的地方////////////////////////
//子窗口保留的问题。
修改保留窗口的算法。
如果可以将进程ID改成16进制的。
//将关于对话里对魔高一丈的感谢语去掉。
//修改版本信息。
修改版本历史文件。
//不隐藏隐身专家所有的子窗口。
//选项对话里热键设置字符串出错。
清除所有的状态应随是否选择列表条目而改变。
//////////////////////////////////2.0版需要修改的地方///////////////////////////